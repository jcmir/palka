[package]
name = "palka-ipc-protocol"
version.workspace = true
edition.workspace = true

[dependencies]
palka-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
