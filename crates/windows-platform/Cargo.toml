[package]
name = "palka-windows-platform"
version.workspace = true
edition.workspace = true

[dependencies]
palka-core = { path = "../core" }

[target.'cfg(windows)'.dependencies.windows]
version = "0.62.2"
features = [
    "Win32_Foundation",
    "Win32_Storage_FileSystem",
]
